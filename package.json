{
  "name": "writings",
  "version": "1.0.0",
  "scripts": {
    "d": "rimraf data/downloaded && ts-node src/download",
    "p": "rimraf data/parsed && ts-node src/parse && prettier --write \"data/parsed/**/*.json\"",
    "e": "rimraf data/extracted && rimraf data/extracted.json && ts-node src/extract && prettier --write \"data/extracted**/*.json\" && prettier --write \"data/extracted.json\""
  },
  "devDependencies": {
    "@types/node": "^12.7.12",
    "fs-extra": "^8.1.0",
    "moment": "^2.24.0",
    "node-fetch": "^2.6.0",
    "prettier": "^1.18.2",
    "rehype-parse": "^6.0.1",
    "rimraf": "^3.0.0",
    "roman-numbers": "^1.0.1",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4",
    "unified": "^8.4.1"
  }
}
